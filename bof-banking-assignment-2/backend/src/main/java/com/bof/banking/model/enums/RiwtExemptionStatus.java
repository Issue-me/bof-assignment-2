package com.bof.banking.model.enums;

/**
 * Holds persisted state and relationships for r iw te xe mp ti on st at us within the domain model.
 */
public enum RiwtExemptionStatus {
    PENDING,
    APPROVED,
    REJECTED
}
